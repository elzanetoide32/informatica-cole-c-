/*Bucle while 3*/
#include <stdio.h>
//gcc for_ej4.c -Wall -o out6

int main (void)
{
    while(1) 
    {
        // Si van a hacer esto, no le pongan un printf() por cada iteración del while() xq ahi probablemente se les cuelgue la computadora 
    }

    return(0);
}

/*
Si alguna vez se les queda loopeando un while 1, presiones "ctrl + c" en la consola y termina el proceso de ejecución de ese programa
Tmb pueden terminar el proceso desde el task manager
*/